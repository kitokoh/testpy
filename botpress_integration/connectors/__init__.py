# This file makes Python treat the `connectors` directory as a package.
# It can be left empty or can contain package-level initializations if needed later.
# For now, it signifies that 'connectors' is a module.
